
import { committeeFormat } from 'enlightened-citzenry/helpers/committee-format';
import { module, test } from 'qunit';

module('Unit | Helper | committee format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = committeeFormat([42]);
  assert.ok(result);
});

